package rooms

templ BuzzerButton (isDisabled bool) {
  <button
    class="buzzer"
    id="buzzer"
    onclick="buzz()"
    disabled?={isDisabled}
  >
    if isDisabled {
      Waiting
    } else {
      Buzz
    }
  </button>
}
